import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App package resolves', () {
    // Placeholder — full widget tests rely on Firebase and are
    // covered by integration testing.
    expect(1 + 1, 2);
  });
}
